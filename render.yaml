services:
  # A Docker web service
  - type: web
    name: udoit
    runtime: image
    image:
      url: ghcr.io/ucfopen/udoit:stable-3.3.x
    plan: free


databases:
  - name: udoit database
    ipAllowList: [] # only allow internal connections
    plan: free
